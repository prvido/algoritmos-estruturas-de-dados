#include <stdio.h>

int main()
{
    int t, a, b;

    scanf("%d %d %d", &t, &a, &b)

    if (a > b)
    {
        int aux = a;
        a = b;
        b = aux;
    }

    int x = 1;
    int tempo = 0;

    while (x != 0)
    {

    }

    return 0;
}